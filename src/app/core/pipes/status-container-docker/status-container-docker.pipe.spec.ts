import { StatusContainerDockerPipe } from './status-container-docker.pipe';

describe('StatusContainerDockerPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusContainerDockerPipe();
    expect(pipe).toBeTruthy();
  });
});
