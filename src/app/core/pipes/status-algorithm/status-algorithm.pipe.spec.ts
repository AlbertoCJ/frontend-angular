import { StatusAlgorithmPipe } from './status-algorithm.pipe';

describe('StatusAlgorithmPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusAlgorithmPipe();
    expect(pipe).toBeTruthy();
  });
});
