import { ZoneLaunchPipe } from './zone-launch.pipe';

describe('ZoneLaunchPipe', () => {
  it('create an instance', () => {
    const pipe = new ZoneLaunchPipe();
    expect(pipe).toBeTruthy();
  });
});
