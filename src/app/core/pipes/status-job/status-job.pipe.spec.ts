import { StatusJobPipe } from './status-job.pipe';

describe('StatusJobPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusJobPipe();
    expect(pipe).toBeTruthy();
  });
});
