import { LinearRegressionBagging } from './linear-regression-bagging';

describe('LinearRegressionBagging', () => {
  it('should create an instance', () => {
    expect(new LinearRegressionBagging()).toBeTruthy();
  });
});
